Create a modern, single-page, mobile-first responsive website that functions as a self-hosted alternative to Beacons.ai or Linktree. It must be built using clean HTML, Tailwind CSS (via CDN), and Vanilla JavaScript so it can be hosted entirely on GitHub Pages without a backend.

The website layout should feature:
1. A top section with a circular profile image, username, and a short bio.
2. A middle section featuring full-width, cleanly styled link buttons with icons.
3. A bottom section containing embedded responsive video players for Twitch and YouTube.

Configuration Requirements:
The entire website content and styling must be driven by a separate configuration file (either a `config.json` or a Markdown file with YAML front matter). JavaScript should fetch this file on load and dynamically render the page. 

The configuration file must allow me to easily edit:
- Profile details (username, bio text, profile image URL)
- Global styles (background color/gradient, text color, button styles)
- An array of links, where each link entry dictates: title, URL, specific icon name, button color, and text color.
- Streaming IDs (Twitch channel name, YouTube Channel ID) and a true/false toggle to hide/show the embeds section entirely.

Embed Implementation Details:
- YouTube Latest Video: The embed must automatically display my latest published video without requiring me to manually update a video ID. Implement this by using the YouTube Embed player with `listType=playlist`. The code should automatically take my YouTube Channel ID from the config file, replace the 'UC' prefix with 'UU' (to target the channel's absolute upload playlist), and inject it into the iframe source URL so the newest upload always renders first.
- Twitch Live Player: Implement the official Twitch interactive iframe embed using the Twitch channel name from the config file. Ensure it is configured correctly with parent domains for GitHub Pages (`username.github.io`). It must natively display my channel's offline "waiting page" banner when offline, and automatically transition to the live stream whenever I am online.

General Technical Details:
- Use Lucide Icons or FontAwesome (via CDN) for the icons. Include standard presets for: ticket, video, map, schedule, and major social media platforms.
- Ensure the layout mimics a sleek mobile app view on desktop (centered container with maximum width, e.g., max-w-md), but scales perfectly to full-width on actual mobile devices.
- Code should be clean, modular, well-commented, and split cleanly into index.html, script.js, and the config file.